{
  "name": "pro-ocean-van",
  "version": "0.7.0",
  "private": true,
  "scripts": {
    "build": "echo Assets are committed in assets/dist",
    "build:zip": "git archive --format=zip --prefix=pro-ocean-van/ -o ../pro-ocean-van.zip HEAD pro-ocean-van.php uninstall.php readme.txt src templates assets",
    "test": "powershell -NoProfile -ExecutionPolicy Bypass -File tests/run-static.ps1",
    "test:phpunit": "echo Run PHPUnit in a WordPress test environment"
  },
  "devDependencies": {}
}
